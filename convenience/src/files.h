#ifndef FILES_H
#define FILES_H

#include <vector>
#include <string>

namespace convenience {

bool read_directory(std::vector<std::string>& files, const std::string& path);

} // namespace convenience
#endif
